You are a senior frontend architect and full-stack engineer.
if this type not exists so you can make it up as long as it is consistent with the existing codebase and follows best practices.
Your task is to FIX and STANDARDIZE the routing, authentication flows, and page structure
for an existing SaaS app:

"AI Workforce Monitoring Platform"

The backend already exists. DO NOT break existing APIs.

====================================================
🎯 GOALS
====================================================

- Fix incorrect routing (admin vs employee login)
- Implement correct authentication flows
- Ensure clean, scalable route structure
- Connect all pages to real backend (NO mock data)
- Maintain fast, smooth UX

====================================================
🚨 STRICT RULES
====================================================

1) DO NOT use static or dummy data
2) DO NOT remove existing functionality unless replacing correctly
3) DO NOT break API contracts
4) Use modular, scalable routing
5) Add proper guards (auth protection)

====================================================
🧭 REQUIRED ROUTE STRUCTURE
====================================================

Implement EXACTLY this routing:

PUBLIC ROUTES:
- /login             → Employee code login (DEFAULT)
- /login/admin       → Admin login (email + password)

PROTECTED ROUTES:
- /dashboard         → Main dashboard
- /devices           → Device list
- /devices/[id]      → Device detail
- /reports           → Reports page

OPTIONAL:
- /settings
- /profile

====================================================
🔐 AUTHENTICATION FLOWS
====================================================

1) EMPLOYEE LOGIN (PRIMARY FLOW)

Route: /login

UI:
- Single input: "Enter Access Code"
- Submit button

Logic:
- POST /auth/verify-code
- Store JWT in local storage or secure store
- Redirect → /dashboard

----------------------------------------------------

2) ADMIN LOGIN

Route: /login/admin

UI:
- Email
- Password

Logic:
- POST /auth/admin/login
- Store JWT
- Redirect → /dashboard

----------------------------------------------------

3) AUTH GUARD (IMPORTANT)

Protect all routes except:
- /login
- /login/admin

If no token:
→ redirect to /login

====================================================
🧱 IMPLEMENTATION DETAILS
====================================================

1) ROUTING SETUP (Next.js)

Use:

pages/
  login.tsx            (employee login)
  login/admin.tsx      (admin login)
  dashboard.tsx
  devices/index.tsx
  devices/[id].tsx
  reports.tsx

----------------------------------------------------

2) AUTH STORE

Create global auth store (Zustand or Context):

- token
- user
- isAuthenticated

Functions:
- login()
- logout()
- setToken()

----------------------------------------------------

3) API SERVICE LAYER

Create reusable API client:

services/api.ts

- attach JWT to headers
- handle errors globally

----------------------------------------------------

4) ROUTE GUARD

Implement wrapper:

- checks token
- redirects if not authenticated

====================================================
🎨 UI REQUIREMENTS
====================================================

EMPLOYEE LOGIN:
- Minimal UI
- Clean input
- Centered layout

ADMIN LOGIN:
- Keep current UI but move to /login/admin

DASHBOARD:
- Must load fast
- Add skeleton loaders
- No blank screens

====================================================
⚡ PERFORMANCE RULES
====================================================

- No full page reloads
- Use client-side navigation
- Cache API responses
- Avoid duplicate API calls

====================================================
🧪 TESTING (MANDATORY)
====================================================

Test ALL flows:

1) Employee:
- Open /login
- Enter code
- Redirect to dashboard

2) Admin:
- Open /login/admin
- Login
- Access dashboard

3) Unauthorized:
- Open /dashboard
- Redirect to /login

4) Logout:
- Clear token
- Redirect to /login

====================================================
🚨 FINAL VALIDATION
====================================================

System is correct ONLY if:

- /login shows code login (NOT admin login)
- /login/admin shows admin login
- All protected routes require auth
- No broken navigation
- UI feels smooth and responsive

====================================================
START NOW:
====================================================

Step 1: Analyze current routing and identify conflicts.
Step 2: Refactor routes to match required structure.
Step 3: Implement employee code login page.
Step 4: Move existing admin login to /login/admin.
Step 5: Add auth guard and test all flows.

DO NOT SKIP TESTING.